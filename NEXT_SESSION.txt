Current handoff - 2026-09-21  (branch: refactor/dashboard-authoritative)

FULL AUDIT + REPAIR PLAN: docs/reports/AUDIT_2026-09-21.md  <- read this first

Owner decisions this session:
- KEEP the VISION.md boundary: lenders get request-code lookup, NOT a
  browsable list of open requests. No marketplace.
- This machine builds bot 2.0. Its database is outdated and not connected to
  the live r/loancentral bot. All migrations were written additive+idempotent
  so they apply cleanly to the live DB when it is reconnected.

Done (8 commits on refactor/dashboard-authoritative, 620 -> 757 tests):
  1. Fixed: any logged-in user could dispute anyone's loan, and the audit row
     named the victim as the actor.
  2. Fixed: update_request_status and report_payment used Postgres E'
',
     a hard syntax error on SQLite - every mod status change on the dev
     dashboard failed. Same class: mark_notifications_read used id=ANY(%s).
  3. Added tests/support/dbcase.RealDBTestCase. ~85% of the old suite mocked
     the DB connection, which is why 1 and 2 shipped green.
  4. schema_migrations tracking + integrity constraints (migration 013) +
     scripts/check_db_integrity.py.
  5. loan_states.py - one lifecycle table for loans and requests. Closes the
     funded -> open -> funded double-funding hole.
  6. Request timeline events moved from routes into services, so the bot and
     $fund produce them. "Funded by u/X" was previously never recorded.
  7. One shared lender gate; structural test that fails if a write route
     ships without a permission check.
  8. Reddit sync: funding queues flair + comment, reddit_sync.py drains with
     retry/backoff, bot stores its own comment id so retries edit rather than
     duplicate. Dry-run unless --live.
  9. Global bans (banned_users) - this did NOT exist before; "$ban" was only
     ever a queued note for a human. Enforced in one before_request hook and
     in the bot; follows linked Reddit aliases.

Second pass (2026-09-22) - made it verifiable, then verified it:
  - SQLite translation now covers INTERVAL / DATE_TRUNC / EXTRACT / casts;
    12 of 30 service functions previously could not run in dev at all.
  - 3 tables existed only in migrations (lender_keys, borrower_otp_sessions,
    borrower_magic_links) - added to schema.sql.
  - FOUND BY RUNNING THE APP: request creation (bot AND dashboard) failed on
    the real loan_requests table (legacy post_date NOT NULL). Inserts now
    adapt to the table's real columns.
  - Title parser: bare "$150" posts were dropped entirely; "Payback" lost the
    repay amount. Both fixed.
  - Bans & Reddit Sync admin page (/dashboard/mod/moderation).
  - Login page no longer shows prod visitors env-var names or a fake
    "Sign in with Reddit".
  - Browser walkthrough of the full flow with zero server errors; mobile ok.
  - Tests: 811, including route sweep (no GET route 500s for any role),
    schema parity, legacy table shape, and the bot driven through main.py.

WHAT IS LEFT IS OWNER-ONLY: docs/LAUNCH_CHECKLIST.md

--------------------------------------------------------------------------

Current handoff - 2026-09-08

- Owner emphasized preserving the existing bot and avoiding account/API risk.
  Keep development local; do not push, deploy, start live Reddit tests, or change
  the other computer's bot as a side effect of development. No ban-proof
  guarantee is possible; verify approved use and shared-account limits before
  a deliberate live test. Do not delete/recreate the account or API registration.
- Request-Code Funding is implemented locally, not pushed/deployed.
- Last code verification: 620 tests passed; desktop/mobile browser flow passed.
- Feature: docs/REQUEST_CODE_FLOW.md; release: docs/REQUEST_CODE_RELEASE.md.
- Community brainstorm: docs/COMMUNITY_RECOVERY.md (draft, not posted).
- New API account is not assumed. Owner prefers existing bot account and EU sub.
- Do not delete/recreate the working bot, its app registration, or its data.
- Owner confirmed the legacy bot runs on a separate computer. Its runtime,
  revision, restart setup, exact EU target, and production login remain to verify.
- Owner confirmed lenders alone mark/update loans. Borrower confirmation is
  not required. Keep that flow; prepare matching pinned instructions for launch.
- Owner reports declining participation due to too few lenders. New-lender
  recruitment waits for the bot release/rebrand. Use existing lenders for pilots.
- Offline title checks found missing bare-dollar principal parsing and missing
  Payback/[REPAY] repayment parsing. These need coverage before live release.
- Existing local demo: http://127.0.0.1:5055 (availability depends on process).
- No production configuration, Reddit content, or credentials changed.

Historical notes below (not current release status):

Latest local dev DB refresh - 2026-06-24

- Active local dashboard URL: http://127.0.0.1:5000
- Dev login page: /auth/dev-login; quick lender login: /auth/dev-login-as/logistix1
- run_dev.py loads .env.test, forces LOANCENTRAL_ENV=dev, and uses DB_NAME=loancentral_dev.
- loancentral_dev is currently the richest accessible data source checked this session: 551 loans.
- Remote .env DB was reachable but only showed 7 loans, so it was not used for dashboard stats.
- Applied existing SQL migrations 001-011 plus missing dev tables/columns for analytics_events, feedback_submissions, notification_preferences, announcements, verified_lender, perm_version, and reddit_username.
- Refreshed u/logistix1 as lender + verified_lender and recomputed its users aggregate from loan rows.
- Verified u/logistix1 lender dashboard shows: 59 recorded loans, 20 active, 37 paid, 0 unpaid, $9,750.00 lent, $5,980.00 received back, $3,625.00 outstanding.

Request Intelligence Sprint — COMPLETE
Date: 2026-06-10
Tests: 554/554 passing

== What was built ==

Task 1 — Request Review Dashboard
  - mod_request_review.html: 5 sections (Open, Duplicates, Expiring Soon, Recently Funded, Removed)
  - Quick actions: View, Status change, Link Loan, Dupe, Remove, Expire (one-click)
  - Route: GET /dashboard/mod/request-review (mod/admin only)

Task 2 — Automation Helpers (admin-run, never auto-delete)
  - flag_possible_duplicates(dry_run) — marks older of duplicate-pair as 'duplicate'
  - flag_missing_thread_links(status_filter) — surface only, no updates
  - flag_unlinked_funded_requests() — surface only, no updates
  - backfill_requests_from_loans(dry_run) — creates funded_backfill records from loans
  - Routes: POST /api/admin/requests/flag-duplicates
           GET  /api/admin/requests/missing-threads
           GET  /api/admin/requests/unlinked-funded
           GET  /api/admin/requests/quality-report
           POST /api/admin/requests/backfill

Task 3 — Data Quality Report
  - generate_request_quality_report() service (read-only)
  - generate_request_dq.py script -> writes REQUEST_DATA_QUALITY.md

Task 4 — Backfill Tool
  - backfill_requests_from_loans(dry_run) in services.py
  - backfill_loan_requests.py script (pass --commit to apply)
  - Status: funded_backfill (added to _LR_STATUSES)

Task 5 — Permissions Review
  - All new admin endpoints behind @require_admin_api (mod gets 403)
  - Review dashboard page behind @role_required("mod","admin")
  - Covered by 12 permission tests in test_request_intelligence.py

Task 6 — UI Polish
  - borrower_requests.html: status chips, thread button, loan button, expiring badge, closed stat card
  - request_detail.html: status chip (large), expiring soon badge, View Thread button,
    Loan #N button (correct URL /dashboard/loans/<id>), back link -> Request Review
  - mod_request_review.html: status chips on all sections

Task 7 — Analytics Polish
  - get_request_analytics() now returns: this_week, this_month, expired_count,
    duplicate_flags, avg_days_to_funded (in addition to prior fields)
  - admin_request_analytics.html: 11 stat cards, flag-duplicates tool, backfill tool

Task 8 — Audit Integration
  - log_request_event() already wired to: create, status_changed, linked_to_loan
  - New event types: unlinked_from_loan, duplicate_flagged, backfilled,
    manually_expired, missing_data_flagged
  - _log_request_event_with_cursor() helper for in-transaction logging

Task 9 — Notification Hooks
  - Borrower: request_recorded (on create)
  - Borrower: request_funded (on status->funded)
  - Borrower: request_expired (on status->expired)
  - Borrower: request_removed (on status->removed)
  - Mods (all): duplicate_request_flagged (on create with dupe / on flag_possible_duplicates)
  - _notify_all_mods() helper queries users WHERE role IN ('mod','admin')

Task 10 — Tests
  - tests/test_request_intelligence.py: 46 new tests
  - Total suite: 554/554 passing

== Key file locations ==
  services.py       -- flag_possible_duplicates, flag_missing_thread_links,
                       flag_unlinked_funded_requests, generate_request_quality_report,
                       backfill_requests_from_loans, _notify_all_mods,
                       _log_request_event_with_cursor, updated get_request_analytics
  api/app.py        -- 5 new admin routes + review dashboard route + notification hooks
  templates/        -- mod_request_review.html (new)
                       admin_request_analytics.html (updated)
                       borrower_requests.html (updated)
                       request_detail.html (updated)
  backfill_loan_requests.py   -- run with --commit to apply
  generate_request_dq.py      -- writes REQUEST_DATA_QUALITY.md

== Pending / not yet done ==
  - Pre-launch blockers H1/H2/H3 (Render snapshots, restore drill, API_KEY)
  - Bot integration for create_loan_request (test-mode stub only)
  - REQUEST_DATA_QUALITY.md not yet generated (run generate_request_dq.py against prod)
