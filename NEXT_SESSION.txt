STATUS: All bot commands link to dashboard. 43/43 tests passing. Dashboards built and ready to test.
LAST THING DONE: Added dashboard links to all 5 bot reply messages. Added search bar to lender
                 dashboard. Deleted legacy dashboard.html. Wired through config.py (DASHBOARD_URL).

NEXT STEPS (in order):
1. Test all 3 dashboards - run server locally, go to /auth/dev-login, try each username:
   - testmod      → mod dashboard (all loans, status tabs, search, role management tab)
   - testlender   → lender dashboard (their loans, stats, search bar, action buttons)
   - testborrower → borrower dashboard (what they owe, health score, full history)
2. Fix anything broken or ugly in the UI
3. Wire up real Reddit OAuth credentials:
   a. Go to https://www.reddit.com/prefs/apps → Create App → type: "web app"
   b. Set redirect URI: http://localhost:5000/auth/callback (or your deployed URL)
   c. Copy into .env:
      DASHBOARD_CLIENT_ID=<your_client_id>
      DASHBOARD_CLIENT_SECRET=<your_client_secret>
      DASHBOARD_REDIRECT_URI=http://localhost:5000/auth/callback
      DASHBOARD_URL=http://localhost:5000   ← bot replies use this too

TO RUN THE SERVER:
1. Open pgAdmin (keep it open so DB is available)
2. Open PowerShell in C:\LB drive
3. Run: python run_dev.py
4. Go to: http://127.0.0.1:5000

GITHUB: https://github.com/TreeHooman/LoanCentral-Test
BRANCH: claude/current-status-PAl0w
TESTS: 43/43 passing (run: python -m unittest discover -s tests -q)

WHAT BOT REPLIES NOW INCLUDE:
- $help         → dashboard link in body + footer line
- $loan         → "View on LoanCentral Dashboard" after lender commands
- $paid_with_id → "View full loan history" link
- $unpaid       → "View loan details" link
- $refunded     → "View updated stats" link
URL comes from DASHBOARD_URL env var (defaults to http://localhost:5000)
