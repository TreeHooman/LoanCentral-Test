================================================================================
LOANCENTRAL — SESSION HANDOFF
Sprint 3: Dashboard UI, Reddit Username Linking, Calendar ICS
Date: 2026-06-09
================================================================================

## SPRINT 3 — COMPLETED ✅

All 8 tasks complete. 109 tests passing (up from 85 in Sprint 2).

---

## WHAT WAS DONE THIS SPRINT

### T1 — Visual Dashboard QA
- All 5 templates reviewed. Four bugs found and fixed in base.html and global_search.html.
- VISUAL_QA_REPORT.md created.
- Fixes:
  1. `escapeHtml()` not defined in base.html → added to main <script> block (XSS risk on timeline)
  2. `result` undeclared in `submitNewLoan()` → added `let result;`
  3. `session_user` undefined → removed, `ME` is sufficient
  4. Global search loan links went to JSON API endpoint → now link to lender user profile

### T2 — Audit Log Viewer UI
- Already complete from prior sprint. Tests added in test_sprint3.py.
- Service: get_audit_log with username filter verified parameterised.

### T3 — Notification Center UI
- Already complete from prior sprint. Tests added in test_sprint3.py.
- get_notifications: verified returns (list, unread_count, error)
- mark_notifications_read: verified scoped to owner username in WHERE clause (IDOR prevention)

### T4 — Global Search UI
- Already complete from prior sprint. Tests added in test_sprint3.py.
- global_search: verified no SQL injection via parameterised queries.
- global_search.html: loan result links fixed (T1 bug 4).

### T5 — Manual Reddit Username Linking
- New feature: mods/admins can link a dashboard username to a Reddit username.
- Schema: reddit_username, reddit_username_linked_at, reddit_username_linked_by added to user_roles.
- local_db.py: _ensure_column migrations for the three new columns.
- services.py: link_reddit_username(), unlink_reddit_username(), get_reddit_username()
  - Normalises u/ prefix automatically
  - Blocks duplicate links (one reddit_username per account)
  - Audit logged via log_audit() (from route layer)
- app.py: GET/POST/DELETE /api/admin/users/<username>/reddit-link (require_mod_api)
- app.py: GET /api/admin/roles now returns reddit_username in response
- dashboard_mod.html: "Reddit" column in roles table, "🔗 Reddit" button per row
- dashboard_mod.html: redditLinkModal — link/unlink with Remove Link button
- No Reddit OAuth, no flair changes, no production credentials touched.

### T6 — Per-Loan ICS Calendar
- New feature: any lender or borrower can download a .ics for a specific loan.
- app.py: GET /api/loans/<loan_id>/calendar.ics (require_auth)
  - Scope check: only lender, borrower, or mod/admin can access
  - Returns 400 if no repay_date set
  - Includes 7-day and 1-day VALARM reminders
- base.html: "📅 Add to Calendar" button in loanDetailModal, wired to ICS endpoint

### T7 — Private Beta Readiness Checklist
- PRIVATE_BETA_CHECKLIST.md created. 8 sections covering security, database,
  Reddit bot, dashboard access, features, language, ops, and deferred items.

### T8 — Final Test Pass
- 109/109 tests passing (24 new tests in test_sprint3.py)
- 0 failures, 1 deprecation warning (utcnow() in ICS helper — cosmetic, not a bug)

---

## FILES CHANGED THIS SPRINT

| File | Change |
|------|--------|
| schema.sql | reddit_username, reddit_username_linked_at, reddit_username_linked_by on user_roles |
| local_db.py | _ensure_column for 3 new reddit_username columns |
| services.py | link_reddit_username, unlink_reddit_username, get_reddit_username functions |
| api/app.py | GET/POST/DELETE /api/admin/users/<username>/reddit-link; GET /api/loans/<id>/calendar.ics; roles API now returns reddit_username |
| api/templates/base.html | escapeHtml() definition; let result; in submitNewLoan; removed session_user; 📅 Add to Calendar button in loanDetailModal |
| api/templates/dashboard_mod.html | Reddit column in roles table; redditLinkModal; JS openRedditLinkModal/saveRedditLink/unlinkReddit |
| api/templates/global_search.html | Loan links now go to lender profile, not JSON API endpoint |
| tests/test_sprint3.py | NEW — 24 tests covering T2/T3/T4/T5/T6 |
| VISUAL_QA_REPORT.md | NEW — QA findings and fix log |
| PRIVATE_BETA_CHECKLIST.md | NEW — 8-section pre-launch checklist |

---

## TEST SUMMARY

| Suite | Tests | Status |
|-------|-------|--------|
| test_audit_log.py | 19 | All pass |
| test_enforcement.py | 14 | All pass |
| test_sprint3.py | 24 | All pass (new) |
| test_dispute_command.py | 5 | All pass |
| test_fund_command.py | 4 | All pass |
| test_help_command.py | 2 | All pass |
| test_integrity.py | 3 | All pass |
| test_loan_command.py | 9 | All pass |
| test_paid_command.py | 10 | All pass |
| test_refund_command.py | 5 | All pass |
| test_services_loan_history.py | 9 | All pass |
| test_unpaid_command.py | 5 | All pass |
| TOTAL | 109 | 109/109 PASSING |

---

## SECURITY CONSTRAINTS (MAINTAINED)

- "Do not trust Reddit flair for permissions. Database remains source of truth." MAINTAINED
- "No production credentials touched." MAINTAINED
- "No Reddit API changes made." MAINTAINED
- "Do not implement Reddit OAuth yet." MAINTAINED

---

## KNOWN ISSUES / DEFERRED

- Reddit OAuth for production login still deferred (see memory/project_auth_future.md)
- Lender API key management UI still missing (endpoint exists; no self-service UI)
- Attachment upload has no server-side size cap enforcement
- `datetime.utcnow()` deprecation warning in ICS route — cosmetic, safe to ignore until Python 3.13+
- Interest fields stored but not prominently displayed in dashboards
- `escapeHtml` still defined locally in dashboard_lender.html (line 731) — now redundant but harmless

---

## NEXT SPRINT RECOMMENDATIONS

### Sprint 4: Borrower UX & Launch Hardening

High priority:
1. Lender verification application flow UI — "Request Verification" button on borrower/lender
   dashboards that calls /api/verification/apply. Currently no UI entry point.

2. Verified Lender badge — show badge on user profiles and in the loan table.
   Language: "Verified Lender (completed LoanCentral process)" per language_audit.md.

3. Notification delivery for key events — extend beyond verification changes to:
   - New loan confirmed
   - Payment received / marked paid
   - Loan marked unpaid
   - Dispute opened

4. Rate limiting on OTP endpoints — /api/auth/borrower/claim and /api/auth/borrower/verify
   should be protected against brute force before launch.

Medium priority:
5. Lender API key self-service UI — let lenders view/revoke their own API keys
   from /dashboard/lender.

6. Server-side attachment upload size cap — add MAX_CONTENT_LENGTH or per-file
   check to the upload endpoint.

7. Reminders dashboard widget — wire /api/reminders to a widget on the lender
   dashboard showing overdue/due-soon loans.

Low priority / housekeeping:
8. Remove redundant escapeHtml from dashboard_lender.html (line 731) — now covered by base.
9. Re-run language audit before launch.
10. Confirm IS_DEV=False in production before first deploy (see PRIVATE_BETA_CHECKLIST.md).
================================================================================
