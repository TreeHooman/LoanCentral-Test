# Test-only Reddit API credentials
# Do not use the production bot account here.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_PASSWORD=
REDDIT_USER_AGENT=LoanCentralTestBot/1.0 by your_test_username

# Test subreddit only
SUBREDDITS=LoanCentralTest

# Test PostgreSQL database only
DB_HOST=localhost
DB_PORT=5432
DB_NAME=loancentral_test
DB_USER=
DB_PASSWORD=

# Test mode flag for future test harness/scripts
LOANCENTRAL_ENV=test

# Safe defaults: tests should not send Reddit comments, DMs, bans, or flair changes
REMINDER_REDDIT_ENABLED=false
AUTO_BAN_REDDIT_ENABLED=false
REDDIT_FLAIR_SYNC_ENABLED=false
